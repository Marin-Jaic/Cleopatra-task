package eu.costengineering.dataprocess.interview;

//Defines all possible CostTypes, I supposed they could be grouped into something like an enum.
//If any new ones arise, simply adding them to the enum will adjust the program to it
public enum CostType {
    DIRECT,
    INDIRECT;
}
